# qa-clean-take-3

A throwaway sandbox for the Altitude surfaces production take. Real code:
- `npm test` — the stats helpers' unit tests (a terminal surface).
- `npm run serve` — a small HTTP dashboard on :3000 (a port surface).
- `npm run build:gallery` — a static stats gallery into `dist-gallery/` (a file/artifact surface).

Prebuild walk of Altitude issue #551, round 2: a push to the default branch wakes the prebuild.
